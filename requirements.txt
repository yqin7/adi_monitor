curl_cffi>=0.5.9
requests>=2.31.0        # app/core/fx.py 取实时汇率；dewu_client 也依赖
pymongo>=4.5.0
python-dotenv>=1.0.0
playwright>=1.40.0
pyyaml>=6.0
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
apscheduler>=3.10,<4
# 得物海外(POIZON)查询客户端，源码见 yqin7/dewu-monitor（私有仓库，独立维护）。
# 只有得物相关任务需要它；缺失时服务仍能启动，比价页读库正常。
# 钉 tag 不钉分支：函数签名一变 batch_price 会直接失败，而调用点包在重试里很难定位。
# 升级流程：dewu-monitor 改完打新 tag → 这里改 tag → pip install -r requirements.txt
# 本地开发想改到即生效：pip install -e ../dewu-monitor（覆盖这一行的安装）
dewu-client @ git+ssh://git@github.com/yqin7/dewu-monitor.git@v1.0.0
